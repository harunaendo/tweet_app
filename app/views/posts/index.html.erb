
<div class="main posts-index">
  <div class="container">
    <% @posts.each do |post| %>
      <div class="posts-index-item">
         <%= link_to(post.content, "/posts/#{post.id}") %><!--コンソールから入力したdbから取り出し-->
      </div>
    <% end %>
  </div>
</div>